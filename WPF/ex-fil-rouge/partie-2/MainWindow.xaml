<Window x:Class="Application_wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Application_wpf"
        mc:Ignorable="d"
        Title="Gestionnaire de notes" Height="650" Width="800">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <!-- COLONNE DE GAUCHE -->
        <Grid Grid.Column="0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Mes notes" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
            
            <ListBox Grid.Row="1" MinHeight="200">
                <ListBoxItem>
                    <StackPanel Margin="0,5">
                        <TextBlock Text="Mathématiques" FontWeight="SemiBold"/>
                        <TextBlock Text="Note: 15/20 - Coef: 4 - Le 10/05/2026" Foreground="Gray" FontSize="12"/>
                    </StackPanel>
                </ListBoxItem>
                <ListBoxItem>
                    <StackPanel Margin="0,5">
                        <TextBlock Text="Physique" FontWeight="SemiBold"/>
                        <TextBlock Text="Note: 12.5/20 - Coef: 3 - Le 12/05/2026" Foreground="Gray" FontSize="12"/>
                    </StackPanel>
                </ListBoxItem>
                <ListBoxItem>
                    <StackPanel Margin="0,5">
                        <TextBlock Text="Informatique" FontWeight="SemiBold"/>
                        <TextBlock Text="Note: 18/20 - Coef: 5 - Le 15/05/2026" Foreground="Gray" FontSize="12"/>
                    </StackPanel>
                </ListBoxItem>
            </ListBox>

            <!-- Formulaire pour ajouter une note -->
            <StackPanel Grid.Row="2" Margin="0,15,0,0">
                <TextBlock Text="Ajouter une note" FontWeight="SemiBold" Margin="0,0,0,5"/>
                
                <TextBlock Text="Matière" FontSize="10" Foreground="Gray"/>
                <TextBox Margin="0,0,0,5"/>
                
                <TextBlock Text="Note" FontSize="10" Foreground="Gray"/>
                <TextBox Margin="0,0,0,5"/>
                
                <TextBlock Text="Coefficient" FontSize="10" Foreground="Gray"/>
                <TextBox Margin="0,0,0,10"/>
                
                <Button Content="Ajouter une note" Height="30"/>
            </StackPanel>
        </Grid>

        <!-- COLONNE DE DROITE -->
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Appréciation générale" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

            <TextBox Grid.Row="1" Margin="0,0,0,10" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                     Text="Trimestre très satisfaisant dans l'ensemble. L'élève démontre un grand intérêt pour les matières scientifiques et l'informatique en particulier. Le travail est régulier et les résultats s'en ressentent. Continuez ainsi !"/>
            
            <Button Grid.Row="2" Content="Enregistrer" HorizontalAlignment="Right" Width="120" Height="35"/>
        </Grid>
    </Grid>
</Window>
