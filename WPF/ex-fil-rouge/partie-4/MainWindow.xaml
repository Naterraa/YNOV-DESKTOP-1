<Window x:Class="Application_wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Application_wpf"
        xmlns:viewmodels="clr-namespace:Application_wpf.ViewModels"
        mc:Ignorable="d"
        Title="Gestionnaire de notes" Height="650" Width="800">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <!-- COLONNE DE GAUCHE -->
        <Grid Grid.Column="0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Mes notes" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
            
            <!-- Liaison de la ListBox à l'ObservableCollection du ViewModel -->
            <ListBox Grid.Row="1" MinHeight="200" ItemsSource="{Binding Notes}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,5">
                            <TextBlock Text="{Binding Matiere}" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Note: " Foreground="Gray" FontSize="12"/>
                                <TextBlock Text="{Binding Valeur}" Foreground="Gray" FontSize="12"/>
                                <TextBlock Text="/20 - Coef: " Foreground="Gray" FontSize="12"/>
                                <TextBlock Text="{Binding Coefficient}" Foreground="Gray" FontSize="12"/>
                                <TextBlock Text=" - Le " Foreground="Gray" FontSize="12"/>
                                <TextBlock Text="{Binding Date, StringFormat=dd/MM/yyyy}" Foreground="Gray" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Formulaire pour ajouter une note avec Binding bidirectionnel -->
            <StackPanel Grid.Row="2" Margin="0,15,0,0">
                <TextBlock Text="Ajouter une note" FontWeight="SemiBold" Margin="0,0,0,5"/>
                
                <TextBlock Text="Matière" FontSize="10" Foreground="Gray"/>
                <TextBox Margin="0,0,0,5" Text="{Binding NouvelleMatiere, UpdateSourceTrigger=PropertyChanged}"/>
                
                <TextBlock Text="Note" FontSize="10" Foreground="Gray"/>
                <TextBox Margin="0,0,0,5" Text="{Binding NouvelleValeur, UpdateSourceTrigger=PropertyChanged}"/>
                
                <TextBlock Text="Coefficient" FontSize="10" Foreground="Gray"/>
                <TextBox Margin="0,0,0,10" Text="{Binding NouveauCoefficient, UpdateSourceTrigger=PropertyChanged}"/>
                
                <Button Content="Ajouter une note" Height="30" Command="{Binding AjouterNoteCommand}"/>
            </StackPanel>
        </Grid>

        <!-- COLONNE DE DROITE -->
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Appréciation générale" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

            <!-- Liaison du TextBox à la propriété AppreciationGenerale -->
            <TextBox Grid.Row="1" Margin="0,0,0,10" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                     Text="{Binding AppreciationGenerale, UpdateSourceTrigger=PropertyChanged}"/>
            
            <Button Grid.Row="2" Content="Enregistrer" HorizontalAlignment="Right" Width="120" Height="35" Command="{Binding EnregistrerCommand}"/>
        </Grid>
    </Grid>
</Window>
